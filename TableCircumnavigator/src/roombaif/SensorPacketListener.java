package roombaif;

/**
 * The sensorPacketReceived method is called whenever a good sensor packet is
 * received from the Roomba.
 *
 */
public interface SensorPacketListener {

    public void sensorPacketReceived();
}
